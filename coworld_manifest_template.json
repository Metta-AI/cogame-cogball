{
  "$schema": "https://raw.githubusercontent.com/Metta-AI/metta/main/packages/coworld/src/coworld/coworld_manifest_schema.json",
  "tags": [
    "cogball",
    "soccer",
    "physics",
    "team",
    "continuous"
  ],
  "episode_timeout_minutes": 20,
  "game": {
    "name": "cogball",
    "replay_viewer": {
      "bundle": "static-replay-viewer"
    },
    "description": "3v3 robot soccer in a continuous 2D physics world. Two seats of three wheeled robots chase one ball on a walled indoor pitch and try to push it over the opponent's goal line; nothing is gridded, and there is no out of play. Every five seconds of match time each seat's coach - an LLM prompt, or one of two scripted baselines - issues ONE directive for all three of its robots (a role, a target point, an intent and a kick policy each), and a deterministic control layer executes it at 30 Hz. Robots are car-like: thrust acts along the heading and lateral velocity is scrubbed off by grip, so facing the right way is part of the skill. The motive is team zero-sum and margin-sensitive: score = 0.5 + 0.5*clamp(goal difference / 3, -1, +1), so the two seats always sum to exactly 1.0. Replays re-simulate from the seed and the recorded action log in a static wasm viewer built from the same C physics core the server runs.",
    "owner": "daveey@softmax.com",
    "runnable": {
      "type": "game",
      "image": "{{GAME_IMAGE}}",
      "run": [
        "/bin/cogball"
      ],
      "source_url": "https://github.com/Metta-AI/cogame-cogball/tree/main"
    },
    "config_schema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "tokens",
        "players"
      ],
      "properties": {
        "tokens": {
          "description": "One connection token per seat, indexed by slot. Injected by the runner; never authored.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "players": {
          "description": "One player display-name object per seat. cogball always seats exactly two: one full trio each.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "name"
            ],
            "properties": {
              "name": {
                "type": "string",
                "minLength": 1
              }
            }
          }
        },
        "seed": {
          "description": "Deterministic sim seed. Omitted: the server derives one and records it in the results and the replay.",
          "type": "integer"
        },
        "max_ticks": {
          "description": "Match length in ticks (30 ticks = 1 s).",
          "type": "integer",
          "minimum": 1,
          "default": 7200
        },
        "turn_ticks": {
          "description": "Ticks per decision turn; the coach issues one directive per turn.",
          "type": "integer",
          "minimum": 1,
          "default": 150
        },
        "turn_budget_seconds": {
          "description": "Wall-clock bound on collecting BOTH seats' directives for one turn; they are issued as one parallel batch.",
          "type": "number",
          "exclusiveMinimum": 0,
          "default": 12
        },
        "tick_deadline_ms": {
          "description": "Per-tick transport deadline.",
          "type": "integer",
          "minimum": 1,
          "default": 1000
        },
        "player_connect_timeout_seconds": {
          "description": "Seconds to wait for both seats before kickoff; a seat that never connects plays the formation baseline and is reported to COGAME_PLAYER_FAILURE_URI.",
          "type": "number",
          "minimum": 0,
          "default": 90
        },
        "wall_clock_budget_seconds": {
          "description": "Engine hard stop: the match ends with reason \"deadline\" once this much real time has elapsed, so results and the replay are always written before the platform's episode timeout kills the container.",
          "type": "number",
          "exclusiveMinimum": 0,
          "default": 690
        },
        "num_agents": {
          "description": "Agent seats; must equal the player slot count. Used by the ladder for block seating.",
          "type": "integer",
          "minimum": 2,
          "maximum": 2,
          "default": 2
        }
      }
    },
    "results_schema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "names",
        "aliases",
        "policy_kinds",
        "scores",
        "win",
        "goals",
        "reason",
        "end_rule",
        "winner",
        "final_tick",
        "final_turn",
        "seed",
        "shots",
        "shots_on_target",
        "saves",
        "passes_completed",
        "interceptions",
        "possession_ticks",
        "distance_m",
        "llm_turns",
        "fallback_turns",
        "fallback_causes"
      ],
      "properties": {
        "names": {
          "description": "One player name per seat, in seat-slot order.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "string"
          }
        },
        "aliases": {
          "description": "The in-game team aliases the seats played as.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "string"
          }
        },
        "policy_kinds": {
          "description": "Per seat: llm (a PLAYER_PROMPT coach) or scripted.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "string",
            "enum": [
              "llm",
              "scripted"
            ]
          }
        },
        "scores": {
          "description": "0.5 + 0.5*clamp(goal_difference/3, -1, +1) per seat; the two seats always sum to exactly 1.0.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "number"
          }
        },
        "win": {
          "description": "Per seat: did this seat finish ahead on goals.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "boolean"
          }
        },
        "goals": {
          "description": "Goals scored by each seat.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "integer",
            "minimum": 0
          }
        },
        "reason": {
          "description": "Why the episode ended.",
          "type": "string",
          "enum": [
            "complete",
            "deadline",
            "fault"
          ]
        },
        "end_rule": {
          "description": "The detail behind `reason`: full time, the mercy rule, the engine's wall-clock hard stop, a sim fault, or an unexpected host error.",
          "type": "string",
          "enum": [
            "full_time",
            "mercy",
            "wall_clock",
            "sim_fault",
            "host_error"
          ]
        },
        "winner": {
          "description": "Winning seat: 0 Azure, 1 Magenta, null draw.",
          "type": [
            "integer",
            "null"
          ],
          "enum": [
            0,
            1,
            null
          ]
        },
        "final_tick": {
          "description": "Sim tick at the final whistle.",
          "type": "integer",
          "minimum": 0
        },
        "final_turn": {
          "description": "Decision turns completed.",
          "type": "integer",
          "minimum": 0
        },
        "seed": {
          "description": "The deterministic sim seed the episode ran with.",
          "type": "integer"
        },
        "shots": {
          "description": "Shots attempted per seat.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "integer",
            "minimum": 0
          }
        },
        "shots_on_target": {
          "description": "Shots whose post-kick ray reached the goal mouth.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "integer",
            "minimum": 0
          }
        },
        "saves": {
          "description": "Shots on target stopped by a defender in its own penalty area.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "integer",
            "minimum": 0
          }
        },
        "passes_completed": {
          "description": "Kicks with intent `pass` collected by a teammate within 120 ticks.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "integer",
            "minimum": 0
          }
        },
        "interceptions": {
          "description": "Passes collected by an opponent instead.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "integer",
            "minimum": 0
          }
        },
        "possession_ticks": {
          "description": "Ticks each seat was the last toucher.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "integer",
            "minimum": 0
          }
        },
        "distance_m": {
          "description": "Metres travelled by each robot, in robot-index order (AZ-1..3 then MG-1..3).",
          "type": "array",
          "minItems": 6,
          "maxItems": 6,
          "items": {
            "type": "number"
          }
        },
        "llm_turns": {
          "description": "Turns each seat's directive came from the LLM coach.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "integer",
            "minimum": 0
          }
        },
        "fallback_turns": {
          "description": "Turns each seat fell back to the scripted layer.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "integer",
            "minimum": 0
          }
        },
        "fallback_causes": {
          "description": "Per-seat fallback cause counters; they sum to fallback_turns.",
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "timeout",
              "parse_error",
              "transport_error",
              "no_credentials",
              "budget_guard"
            ],
            "properties": {
              "timeout": {
                "type": "integer",
                "minimum": 0
              },
              "parse_error": {
                "type": "integer",
                "minimum": 0
              },
              "transport_error": {
                "type": "integer",
                "minimum": 0
              },
              "no_credentials": {
                "type": "integer",
                "minimum": 0
              },
              "budget_guard": {
                "type": "integer",
                "minimum": 0
              }
            }
          }
        }
      }
    },
    "protocols": {
      "player": {
        "type": "uri",
        "value": "https://github.com/Metta-AI/cogame-cogball/blob/main/docs/PROTOCOL.md"
      },
      "global": {
        "type": "uri",
        "value": "https://github.com/Metta-AI/cogame-cogball/blob/main/docs/PROTOCOL.md"
      }
    },
    "docs": {
      "readme": {
        "type": "text",
        "value": "# cogball\n\n**3v3 robot soccer in a continuous 2D physics world.** Two teams of three wheeled robots chase\none ball on a walled indoor pitch and try to push it over the opponent's goal line. Nothing is\ngridded: positions, velocities and headings are IEEE doubles; the only discrete things in the\nworld are the tick and the kick.\n\nCogball is a Coworld. A policy is just a prompt: every 5 seconds of match time a coach — an LLM,\nor one of two scripted baselines — issues **one directive** for all three of its robots (a role, a\ntarget point, an intent and a kick policy each), and a deterministic control layer executes it at\n30 Hz. The coach is the tactics; the control layer is the reflexes.\n\n```\nSeats            2 (num_agents = 2). One seat = one full trio.\n                 Seat 0 Azure  (AZ-1..3) defends x = -20, attacks +x\n                 Seat 1 Magenta (MG-1..3) defends x = +20, attacks -x\nPitch            x in [-20, +20], y in [-12.5, +12.5]; goal mouths |y| <= 3.5 at x = +-20\nTime             30 ticks/s, 4 physics substeps per tick, 7200 ticks = 4:00 of soccer\nTurns            48 decision turns of 150 ticks (5.0 s)\nMotive           team zero-sum: score(seat) = 0.5 + 0.5 * clamp(goal_difference / 3, -1, +1)\n                 so the two seats' scores always sum to exactly 1.0\n```\n\n## How a match plays\n\nAt the start of each turn the server freezes the world, sends each seat its view (the complete\nphysical state — soccer is a perfect-information sport — plus the score, the clock and its own\nlast directive), and asks both coaches **in one parallel batch** what to do. Each reply is a\nsingle JSON object:\n\n```json\n{\"note\": \"compact, keeper stays home\",\n \"robots\": [{\"id\": \"AZ-1\", \"role\": \"keeper\", \"intent\": \"hold\", \"target\": [-17.0, 0.4],\n             \"pass_to\": null, \"kick\": \"auto\", \"say\": \"holding the arc\"}, \"... and two more\"]}\n```\n\nIntents are `chase`, `intercept`, `hold`, `shoot`, `pass`, `clear` and `press`. The control layer\nturns each one into a steering point, then into thrust and turn commands: robots are car-like, so\nlateral velocity is scrubbed off by grip and **facing the right way is part of the skill**.\n\nA reply that is late, malformed or unusable is retried once and then replaced by the `formation`\nbaseline's directive — the match never stalls waiting for a coach.\n\n## Policies\n\nOne image, switched by environment variable:\n\n| env | what the seat plays |\n| --- | --- |\n| `PLAYER_PROMPT=<strategy text>` | an LLM coach; the prompt *is* the policy |\n| `PLAYER_SCRIPTED=formation` | keeper on the arc, striker on the ball, third robot shuttling |\n| `PLAYER_SCRIPTED=swarm` | everyone chases; deliberately weaker |\n| neither | `formation` |\n\nBoth shipped champions (`cogball-total`, `cogball-counter`) are prompt policies.\n`docs/COACHING.md` is the guide to writing one.\n\n## Determinism and replays\n\nThe physics core (`sim/cogball_core.c`, ~400 lines of C99) uses **only `+ - * / sqrt`** on\ndoubles — no libm, no `float`, no `-ffast-math`. WebAssembly specifies those operations exactly\nand forbids contraction, so the server build (wasmtime, standalone wasm) and the browser build\n(emscripten) produce **bit-identical** state. That is what lets a replay be nothing more than a\nseed plus an action log:\n\n```\nreplay = {seed, first_kickoff_seat, controls_b64, keyframes[…digests…], events[…], results}\n```\n\n`tests/test_determinism.py` is the inviolable gate: identical digests at every 30-tick keyframe,\nacross both toolchains. If it fails, the physics or a build flag changed — fix the code, never\nthe test.\n\nThe replay viewer is a **static wasm bundle** (`viewer/`), built from the same C source by\n`tools/build_replay_viewer.sh`. It re-simulates in the browser and draws the pitch, the robots,\nball trails, kick rings, goal fireworks, an instant slow-motion goal replay, and a persistent\nposition-history heat trail per robot — over four minutes the keeper's arc, the back's shuttle\nand the striker's runs separate visually, so you can see roles emerge with no labels on screen.\n\n## Layout\n\n```\nsim/            cogball_core.{c,h}, cogball_config.h, viewer_main.c, build_sim.sh, build_viewer.sh\nserver/cogball/ server.py engine.py sim.py control.py directives.py baselines.py llm.py\n                replay.py config.py defaults.py uris.py\nplayers/        cogball_player.py    (register once, then watch)\nviewer/         index.html, static_replay.js  -> viewer/dist (wasm bundle)\ntests/          physics, determinism, control, baselines, llm parsing, engine, replay,\n                server, manifest, viewer, startup, perf\ntools/          build_replay_viewer.sh, ci/docker_smoke.sh, ci/policies.json\ndocs/           PROTOCOL.md, COACHING.md, plans/\n```\n\n## Build and test\n\n```sh\nbash sim/build_sim.sh       # -> build/cogball_sim.wasm      (wasmtime host)\nbash sim/build_viewer.sh    # -> viewer/dist/ + build/viewer_core.{js,wasm}\nuv sync && uv run pytest\n```\n\nBoth build scripts need emscripten; the viewer build also downloads the sha-pinned raylib 5.5 web\nrelease. CI (`.github/workflows/ci.yml`) runs all of it, plus a raw-Docker episode smoke and the\nstatic replay-viewer bundle build.\n"
      },
      "pages": [
        {
          "id": "protocol.md",
          "title": "Wire protocol",
          "content": {
            "type": "text",
            "value": "# cogball wire protocol\n\nEverything a seat can see, and everything it may send. The authority for the\nruntime contract is `server/cogball/server.py`; this page is the human-readable\nversion of it.\n\n## Runtime contract (Coworld `COGAME_*`)\n\n| variable | meaning |\n| --- | --- |\n| `COGAME_CONFIG_URI` | the episode config JSON (required in episode mode) |\n| `COGAME_RESULTS_URI` | where `results.json` is written |\n| `COGAME_SAVE_REPLAY_URI` | where the replay JSON is written |\n| `COGAME_PLAYER_FAILURE_URI` | where a no-show seat is declared |\n| `COGAME_LOAD_REPLAY_URI` | set ⇒ replay mode: serve a recorded replay, run no episode |\n| `COGAME_HOST` / `COGAME_PORT` | bind address (default `0.0.0.0:8080`) |\n\nRoutes: `GET /healthz`, `GET /player?slot=N&token=T` (websocket),\n`GET /global` (broadcast websocket), `GET /client/global`,\n`GET /client/player`, and in replay mode `GET /replay-data` +\n`GET /client/replay`. A bad slot or token is `403`; a second connection to an\nalready-connected slot is `409`.\n\n## The episode config\n\n```json\n{\"seed\": 2864434397, \"max_ticks\": 7200, \"turn_ticks\": 150,\n \"turn_budget_seconds\": 12, \"tick_deadline_ms\": 1000,\n \"player_connect_timeout_seconds\": 90, \"wall_clock_budget_seconds\": 690,\n \"num_agents\": 2,\n \"players\": [{\"name\": \"Azure\"}, {\"name\": \"Magenta\"}],\n \"tokens\": [\"token-0\", \"token-1\"]}\n```\n\n`seed` may be omitted; the server derives one and records it in the results and\nthe replay. `num_agents`, if present, must equal the seat count.\n\n## What a player sends: one frame, ever\n\nDecisions are made by the **game server** — the hosted Bedrock credential and\nthe `anthropic_api_key` Coworld secret are injected into the game pod — so a\nplayer container is thin. On connect it sends exactly one frame and then only\nreceives:\n\n```json\n{\"type\": \"register\",\n \"prompt\": \"<strategy text, or empty>\",\n \"scripted\": \"formation\" | \"swarm\" | null,\n \"policy\": \"<free label, <= 48 runes>\"}\n```\n\nA non-empty `prompt` makes the seat an **LLM seat**: that text is the policy,\nand the server asks Claude for a directive every turn on the seat's behalf. A\nseat that registers with neither field — or never registers at all, or never\nconnects — plays the `formation` baseline. An over-long `prompt` is truncated\nto 4 000 runes, not rejected; it is never written to the replay or the results.\n\n## What the server sends\n\nOnce per decision turn (every `turn_ticks` = 150 ticks = 5.0 s):\n\n```json\n{\"type\": \"turn\", \"turn\": 7, \"tick\": 1050, \"view\": { … },\n \"directive_source\": \"llm\"}\n```\n\nand at the end, followed by close:\n\n```json\n{\"done\": true, \"result\": { …the results document… }}\n```\n\n`GET /global` is broadcast-only: a status snapshot on connect, a throttled\n`{\"tick\": t}` progress feed, then the same done message.\n\n## The per-seat view\n\nNumbers are rounded to 2 decimals.\n\n```json\n{\"turn\": 7, \"of\": 48, \"clock\": {\"played_s\": 35.0, \"left_s\": 205.0},\n \"score\": {\"you\": 1, \"them\": 0},\n \"you\": {\"alias\": \"Azure\", \"attacking_x\": \"+20\", \"defending_x\": \"-20\"},\n \"pitch\": {\"x_min\": -20, \"x_max\": 20, \"y_min\": -12.5, \"y_max\": 12.5,\n           \"goal_half_width\": 3.5, \"your_penalty_area\": \"x <= -14, |y| <= 7\"},\n \"ball\": {\"pos\": [3.21, -1.04], \"vel\": [4.1, 0.62], \"speed\": 4.15,\n          \"possession\": \"AZ-2\", \"in_your_half\": false},\n \"your_robots\": [{\"id\": \"AZ-1\", \"pos\": [-16.9, 0.4], \"vel\": [0.2, -0.1],\n                  \"facing\": [1.0, 0.0], \"speed\": 0.22, \"kick_ready\": true,\n                  \"dist_to_ball\": 20.1, \"last_role\": \"keeper\"}],\n \"their_robots\": [{\"id\": \"MG-1\", \"pos\": [7.7, 2.1], \"vel\": [-3.0, 0.4],\n                   \"facing\": [-0.99, 0.13], \"speed\": 3.03,\n                   \"dist_to_ball\": 5.6}],\n \"last_turn\": {\"your_kicks\": 2, \"their_kicks\": 1, \"your_shots\": 1,\n               \"their_shots\": 0, \"goals\": [], \"possession_pct_you\": 63},\n \"your_last_directive\": { … or null on turn 0 … }}\n```\n\n**Visible:** the whole physical state (soccer is a perfect-information sport),\nthe score, the clock, the turn index, the seat's own previous directive, and\nlast-turn event counts.\n\n**Hidden:** the opponent's directives, roles, intents, `note`/`say` text and\nprompt — never, not even after the fact; the episode seed; real player names\n(a seat sees only `Azure` / `Magenta` and robot ids); the opponent's policy\nkind; and the future. Spectators see all of it in the replay. That asymmetry\nis deliberate.\n\n## The directive (what a coach replies)\n\n```json\n{\"note\": \"compact, keeper stays home\",\n \"robots\": [{\"id\": \"AZ-1\", \"role\": \"keeper\", \"intent\": \"hold\",\n             \"target\": [-17.0, 0.4], \"pass_to\": null, \"kick\": \"auto\",\n             \"say\": \"holding the arc\"}]}\n```\n\nexactly three entries, one per robot.\n\n| field | legal values | repair when violated |\n| --- | --- | --- |\n| `note` | string, ≤ **160 runes** | truncated to 160 runes |\n| `robots` | exactly the seat's 3 robots | extras dropped; missing ids filled from last turn, else from `formation` |\n| `robots[].id` | `AZ-1..3` / `MG-1..3`, case-insensitive, ≤ 8 runes | unmatched entries assigned by position |\n| `robots[].role` | `keeper` `back` `wing` `striker` | → `wing` |\n| `robots[].intent` | `chase` `intercept` `hold` `shoot` `pass` `clear` `press` | → `chase` |\n| `robots[].target` | `[x, y]`, finite | clamped to the pitch; non-finite/missing → the robot's current position |\n| `robots[].pass_to` | a *teammate* id ≠ self, or null | → null (and `pass` degrades to `shoot`) |\n| `robots[].kick` | `auto` `never` | → `auto` |\n| `robots[].say` | string, ≤ **48 runes** | truncated to 48 runes |\n\nParsing is tolerant: markdown fences are stripped, the outermost balanced\n`{…}` is taken if the model prefixed prose, `robots` may be an object keyed by\nid, and numeric strings are accepted for `target`. Only if no object with at\nleast one usable robot entry can be recovered does the **one** retry fire, and\nthen the `formation` fallback.\n\n**Every string that reaches the replay is truncated on rune (codepoint)\nboundaries, never bytes.** A byte-truncated multi-byte character is exactly\nthe bug that makes replay bytes render in a browser but fail a strict JSON\nparser.\n\n## Timing\n\n| bound | value |\n| --- | --- |\n| decision turn | 150 ticks (5.0 s of sim time), 48 per match |\n| first LLM attempt | 8.0 s |\n| retry | 3.5 s |\n| whole turn (both seats, one batch) | 12.0 s |\n| player connect wait | `player_connect_timeout_seconds` (90 s) |\n| engine hard stop | `wall_clock_budget_seconds` (690 s) → `reason: \"deadline\"` |\n| final done broadcast, per seat | 3.0 s |\n\nBoth seats are queried in **one parallel batch** per turn (a single\n`asyncio.gather` under a single `asyncio.wait_for`), so a match costs 48 × 12 s\nof coaching at worst, not 96 × 12 s. If two more full turn budgets would\noverrun the hard stop, a **budget guard** drops the rest of the match onto the\nscripted layer, so the episode ends `complete`/`full_time` rather than\n`deadline`.\n\n## The results document (closed schema)\n\n```json\n{\"names\": [\"daveey\", \"daveey-1\"], \"aliases\": [\"Azure\", \"Magenta\"],\n \"policy_kinds\": [\"llm\", \"scripted\"], \"scores\": [0.667, 0.333],\n \"win\": [true, false], \"goals\": [1, 0], \"reason\": \"complete\",\n \"end_rule\": \"full_time\", \"winner\": 0, \"final_tick\": 7200, \"final_turn\": 48,\n \"seed\": 2864434397, \"shots\": [7, 4], \"shots_on_target\": [3, 1],\n \"saves\": [1, 3], \"passes_completed\": [9, 5], \"interceptions\": [4, 6],\n \"possession_ticks\": [4120, 3080],\n \"distance_m\": [211.4, 340.2, 298.7, 205.9, 318.0, 331.6],\n \"llm_turns\": [48, 0], \"fallback_turns\": [0, 0],\n \"fallback_causes\": [{\"timeout\": 0, \"parse_error\": 0, \"transport_error\": 0,\n                      \"no_credentials\": 0, \"budget_guard\": 0}, {}]}\n```\n\n`score(seat) = 0.5 + 0.5 · clamp(goal_difference / 3, −1, +1)`, and the two\nseats always sum to exactly 1.0. `reason` is one of `complete`, `deadline`,\n`fault`; `end_rule` carries the detail (`full_time`, `mercy`, `wall_clock`,\n`sim_fault`, `host_error`).\n\n## The replay\n\nUTF-8 JSON, `protocol: \"cogball/v1\"`. `seed` + `first_kickoff_seat` +\n`controls_b64` (tick_count × 18 bytes of quantised `(thrust i8, turn i8,\nkick u8)` per robot) + the pinned physics core reproduce the episode exactly.\n`keyframes` carry the state digest every 30 ticks so a re-simulation can be\nverified without trusting it, and `events` carry the human-readable story:\n`match_start`, `turn_start`, `directive`, `fallback`, `budget_guard`, `kick`,\n`touch`, `shot`, `save`, `pass_completed`, `interception`, `post`, `goal`,\n`kickoff`, `turn_end`, `end`.\n"
          }
        },
        {
          "id": "coaching.md",
          "title": "Writing a cogball prompt",
          "content": {
            "type": "text",
            "value": "# Writing a cogball prompt\n\nA cogball policy is a prompt. You are not writing motor commands; you are\nwriting a **coach**. Every five seconds of match time your prompt, plus the\ncurrent state of the pitch, goes to the model, and the model answers with one\ndirective for all three of your robots. A deterministic controller then\nexecutes that directive at 30 Hz for the next five seconds.\n\nSo the question your prompt has to answer is: *given the ball here and my\nthree robots there, who does what?*\n\n## What the model sees\n\nYour prompt, then a blank line, then the seat's view as JSON: ball position,\nvelocity and speed; every robot's position, velocity, heading, speed and\ndistance to the ball; whether each of your robots' kickers are off cooldown;\nthe score; the clock; last turn's kick/shot counts and possession share; and\nthe directive you played last turn. It sees `Azure` / `Magenta` and robot ids\n`AZ-1..3` / `MG-1..3` — never the real player behind the other seat.\n\n## What it must answer\n\n```json\n{\"note\": \"compact, keeper stays home\",\n \"robots\": [\n   {\"id\": \"AZ-1\", \"role\": \"keeper\", \"intent\": \"hold\", \"target\": [-17, 0.4],\n    \"pass_to\": null, \"kick\": \"auto\", \"say\": \"holding the arc\"},\n   {\"id\": \"AZ-2\", \"role\": \"striker\", \"intent\": \"shoot\", \"target\": [12, 1],\n    \"pass_to\": null, \"kick\": \"auto\", \"say\": \"going for goal\"},\n   {\"id\": \"AZ-3\", \"role\": \"wing\", \"intent\": \"intercept\", \"target\": [6, -5],\n    \"pass_to\": null, \"kick\": \"auto\", \"say\": \"running the channel\"}]}\n```\n\nExactly three entries, one per robot. The system prompt already states the\nschema, so your prompt should spend its words on **tactics**, not on JSON.\n\n## The seven intents, and what the controller actually does with each\n\n| intent | the controller drives the robot to… | use it when |\n| --- | --- | --- |\n| `chase` | the ball itself | you just want a body on it |\n| `intercept` | where the ball *will be* (up to 1.5 s of lead) | a loose ball, a cut-out |\n| `hold` | your `target`, then turns to face the ball | keepers, backs, anyone who must not be dragged out |\n| `shoot` | 0.9 m behind the ball on the line to their goal, then strikes | the ball is in front of goal |\n| `pass` | 0.9 m behind the ball on the line to `pass_to` | you have a free teammate |\n| `clear` | 0.9 m behind the ball on the line up the touchline | the ball is in your own box |\n| `press` | the opponent nearest the ball, plus half a second of lead | you want to deny time, not win the ball |\n\n`target` is used **directly** by `hold`; for every other intent it is a 20 %\nbias on the steering point, which is how you nudge a chaser to favour one side\nof the pitch. `kick: \"never\"` makes a robot shepherd the ball instead of\nstriking it — useful for a robot you want to keep possession with, expensive if\nyou use it on your striker.\n\n## Five things that decide matches\n\n1. **Never leave the goal empty.** One robot on `hold` at roughly\n   `(±17, ball_y / 3)` covers the near post and is worth more than a third\n   attacker. Every strong cogball prompt names a keeper every single turn.\n2. **Robots are car-like.** Thrust acts along the heading and lateral velocity\n   is scrubbed off by grip, so a robot that has to turn 180° loses about a\n   second. Sending the *nearest* robot at the ball beats sending the *best*\n   one.\n3. **Rotate roles by distance, not by name.** The robot nearest the ball\n   should be the one attacking it this turn, whichever id that is. Say so in\n   the prompt: \"the nearest robot always attacks, the deepest always keeps\".\n4. **A shot from outside 12 m is a giveaway.** The ball loses most of its pace\n   in about three seconds; a long shot arrives slowly and hands over\n   possession. Prefer `pass` to a supporting robot.\n5. **Five seconds is a long time.** Whatever you order will still be running\n   five seconds from now. Prefer intents that track the ball live (`chase`,\n   `intercept`, `shoot`, `press`) for the robot near the action, and `hold`\n   with an explicit target for the robots that should stay where they are.\n\n## Things that will not work\n\n- Micro-managing motors. There is no thrust or steering field; the controller\n  owns that.\n- Referring to \"the last time we played\" — nothing carries between episodes.\n- Talking to the other seat. `note` and `say` are one-way, into the replay\n  feed. (They are the only thing a spectator sees of your reasoning, so a good\n  `note` is genuinely worth writing.)\n- Asking for a formation by name. Say who holds where and who goes for the\n  ball; the schema has no formation field.\n\n## The two shipped champions\n\n`cogball-total` plays total football: always a keeper, always a presser, a\nthird robot running ahead of the ball into space, roles rotating by distance,\nand a preference for the pass over the low-percentage shot.\n\n`cogball-counter` plays the counter: two robots behind the ball line holding\nstation, one pressing; the moment its team wins the ball, the presser shoots,\nthe back breaks wide, and only the keeper stays home. It never sends all three\nrobots past halfway.\n\nBoth are pure prompts against the same image and the same controller. If you\nwant to beat them, the fastest lever is not a cleverer shape — it is being\nmore decisive about *who* attacks and *who* stays.\n"
          }
        }
      ]
    }
  },
  "player": [
    {
      "id": "baseline",
      "name": "formation",
      "type": "player",
      "description": "The bundled certification player: keeper on the goal arc, striker on the ball, third robot shuttling between cover and the channel. Pure scripted logic, no LLM, so certification completes offline in seconds.",
      "image": "{{PLAYER_IMAGE}}",
      "run": [
        "/bin/cogball-player"
      ],
      "env": {
        "PLAYER_SCRIPTED": "formation"
      },
      "source_url": "https://github.com/Metta-AI/cogame-cogball/tree/main/players",
      "resources": {
        "requests": {
          "cpu": "250m",
          "memory": "256Mi"
        },
        "limits": {
          "cpu": "1"
        }
      }
    }
  ],
  "variants": [
    {
      "id": "default",
      "name": "Match (2 seats x 3 robots, 4:00)",
      "description": "The full match: 7200 ticks, 48 decision turns.",
      "game_config": {
        "players": [
          {
            "name": "Azure"
          },
          {
            "name": "Magenta"
          }
        ],
        "num_agents": 2,
        "max_ticks": 7200,
        "turn_ticks": 150,
        "turn_budget_seconds": 12,
        "tick_deadline_ms": 1000,
        "player_connect_timeout_seconds": 90,
        "wall_clock_budget_seconds": 690
      }
    },
    {
      "id": "sprint",
      "name": "Sprint (2 seats x 3 robots, 2:00)",
      "description": "Half-length match for cheap ladder rounds; same two seats, same three robots each, only the clock changes.",
      "game_config": {
        "players": [
          {
            "name": "Azure"
          },
          {
            "name": "Magenta"
          }
        ],
        "num_agents": 2,
        "max_ticks": 3600,
        "turn_ticks": 150,
        "turn_budget_seconds": 12,
        "tick_deadline_ms": 1000,
        "player_connect_timeout_seconds": 90,
        "wall_clock_budget_seconds": 400
      }
    }
  ],
  "certification": {
    "players": [
      {
        "player_id": "baseline"
      },
      {
        "player_id": "baseline"
      }
    ],
    "game_config": {
      "players": [
        {
          "name": "Azure"
        },
        {
          "name": "Magenta"
        }
      ],
      "num_agents": 2,
      "seed": 42,
      "max_ticks": 900,
      "turn_ticks": 150,
      "turn_budget_seconds": 12,
      "tick_deadline_ms": 1000,
      "player_connect_timeout_seconds": 60,
      "wall_clock_budget_seconds": 180
    }
  }
}
